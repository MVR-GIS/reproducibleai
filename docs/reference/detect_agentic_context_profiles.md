# Detect applicable agentic-context profiles

Detection is advisory. It returns evidence but never changes a
repository or silently selects a profile for scaffolding.

## Usage

``` r
detect_agentic_context_profiles(path = ".")
```

## Arguments

- path:

  Existing repository root.

## Value

A data frame with candidate profiles and evidence.
