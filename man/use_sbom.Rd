% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_sbom.R
\name{use_sbom}
\alias{use_sbom}
\title{Configure an R package repository to generate SBOM artifacts in CI}
\usage{
use_sbom(dest_dir = ".", overwrite = FALSE, quiet = FALSE)
}
\arguments{
\item{dest_dir}{Character. Path to the repository root. Defaults to \code{"."}.}

\item{overwrite}{Logical. Overwrite an existing workflow file? Defaults to \code{FALSE}.}

\item{quiet}{Logical. Suppress informational messages? Defaults to \code{FALSE}.}
}
\value{
Character vector of written/updated file paths (invisibly).
}
\description{
Writes a GitHub Actions workflow (\code{.github/workflows/sbom-r.yml}) that generates
an SBOM using Anchore's SBOM Action (Syft-based), and writes additional
environment metadata (and a copy of \code{renv.lock}) into \verb{artifacts/sbom/}.
}
\details{
This function is intended to be run during initial repository setup.

What this function does:
\itemize{
\item Validates that \code{dest_dir} is an R package root (has \code{DESCRIPTION}).
\item Creates \verb{.github/workflows/} if missing.
\item Writes \code{.github/workflows/sbom-r.yml} (fails if it exists and \code{overwrite = FALSE}).
\item Ensures ignore rules:
\itemize{
\item Adds \verb{/artifacts/} to \code{.gitignore}
\item Adds \verb{^artifacts$} to \code{.Rbuildignore}
}
}

Next steps after running:
\itemize{
\item Ensure \code{reproducibleai} is included in the project's \code{renv.lock}
(install it and run \code{renv::snapshot()} if needed).
\item Commit changes and open a PR to verify the workflow produces an SBOM bundle.
}
}
