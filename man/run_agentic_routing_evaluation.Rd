% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agentic-routing-run.R
\name{run_agentic_routing_evaluation}
\alias{run_agentic_routing_evaluation}
\title{Run repeated agentic-routing evaluations}
\usage{
run_agentic_routing_evaluation(
  path = ".",
  questions,
  repetitions = 3L,
  approved = FALSE,
  output_dir = NULL,
  codex = NULL,
  model = NULL,
  timeout = 900,
  runner = NULL,
  quiet = FALSE
)
}
\arguments{
\item{path}{Git repository to evaluate.}

\item{questions}{One question or a list of questions created by
\code{new_agentic_routing_question()}.}

\item{repetitions}{Positive number of independent runs per question.}

\item{approved}{Must be \code{TRUE}. Repeated model execution can consume paid
usage and is never started implicitly.}

\item{output_dir}{Directory for raw event and response files. It must be
outside \code{path}.}

\item{codex}{\code{NULL} to discover a standalone Codex CLI automatically, or a
CLI command or absolute executable path. Live evaluation requires this
optional external program and existing CLI authentication. Use
\code{check_agentic_routing_prerequisites()} before the first run.}

\item{model}{Optional model override. \code{NULL} uses the configured default.}

\item{timeout}{Maximum seconds for each run.}

\item{runner}{Optional process runner for testing or custom execution. It
receives \code{command}, \code{args}, \code{wd}, \code{stdout}, \code{stderr}, and \code{timeout}.}

\item{quiet}{Suppress progress messages.}
}
\value{
An object of class \code{agentic_routing_evaluation}.
}
\description{
Runs each competency question in a fresh, ephemeral, read-only \verb{codex exec}
session and captures JSONL events plus schema-constrained final responses.
Gold rubrics are never included in model prompts.
}
\details{
Raw output defaults to a temporary directory outside the target repository.
This prevents earlier results from contaminating later repetitions. Live
execution also requires a clean Git repository and verifies that its commit
and worktree remain unchanged across the run.
}
